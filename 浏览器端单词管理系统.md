# 浏览器端单词管理系统

# 浏览器端单词管理系统软件需求设计文档



## 1. 引言



### 1.1 项目背景



随着英语学习需求的增长，用户需要一个轻量化、易操作的工具来管理英语单词。现有工具多需安装客户端或功能复杂，而用户期望通过浏览器直接操作，实现单词的动态维护、标签化管理、精准搜索及结构化导出，以提升单词学习效率。



### 1.2 项目目的



开发一款浏览器端单词管理系统，满足用户以下核心需求：  



- 动态维护英语单词及其中文释义  

- 支持自定义标签并关联单词  

- 可通过标签快速搜索单词  

- 按指定格式导出包含复习记录的Excel文件  



### 1.3 项目范围



- **适用环境**：主流浏览器（Chrome、Edge、Firefox、Safari最新版本）  

- **用户群体**：个人英语学习者  

- **核心功能**：单词维护、标签管理、标签搜索、Excel导出  

- **数据存储**：浏览器本地存储（localStorage），无需后端服务器  



## 2. 功能需求



### 2.1 单词管理模块



#### 2.1.1 单词添加



- 支持手动输入单词、中文释义  

- 可同时为单词关联1个或多个标签（支持选择已有标签或创建新标签）  

- 提交后自动生成唯一单词序号（按添加顺序递增）  

- 初始状态下，6次复习记录为空（待用户后续标记）  



#### 2.1.2 单词编辑



- 支持修改单词的拼写、中文释义  

- 支持调整关联的标签（添加/移除标签）  

- 支持更新6次复习记录的状态（如“已完成”“未完成”，以文本或勾选标记存储）  



#### 2.1.3 单词删除



- 支持单个单词删除，删除时需二次确认（避免误操作）  

- 单词删除后，其关联的标签关系同时删除（标签本身保留，除非手动删除标签）  



#### 2.1.4 单词展示



- 以列表形式展示所有单词，包含字段：单词序号、单词、中文释义、关联标签、6次复习记录状态  

- 支持列表排序（按序号升序/降序，默认按序号升序）  



### 2.2 标签管理模块



#### 2.2.1 标签创建



- 支持自定义标签名称（如“考研”“日常词汇”“动词”等）  

- 标签名称不可重复（系统自动校验，重复时提示“标签已存在”）  



#### 2.2.2 标签编辑



- 支持修改标签名称（修改后，关联该标签的单词自动同步更新）  



#### 2.2.3 标签删除



- 支持单个标签删除，删除前需提示“该标签关联X个单词，删除后单词将移除该标签，是否继续？”  

- 标签删除后，仅解除与单词的关联，不影响单词本身  



#### 2.2.4 标签展示



- 以标签云或列表形式展示所有标签，显示标签名称及关联单词数量  



### 2.3 搜索模块



- 支持通过标签搜索单词：用户选择1个或多个标签，系统展示同时关联所有选中标签的单词  

- 搜索结果以列表形式展示（同2.1.4的字段）  

- 支持搜索结果的排序（同2.1.4）  



### 2.4 导出模块



- 支持将单词数据导出为Excel文件（.xlsx格式）  

- 导出文件表头固定为：`单词序号 | 单词 | 中文释义 | 第1次复习 | 第2次复习 | 第3次复习 | 第4次复习 | 第5次复习 | 第6次复习`  

- 导出内容包含当前系统中所有单词（搜索状态下可选择导出“搜索结果”或“全部单词”）  

- 导出时自动命名文件为“单词管理记录_YYYYMMDD.xlsx”（YYYYMMDD为导出当天日期）  



## 3. 非功能需求



### 3.1 易用性



- 界面简洁直观，操作流程清晰（如添加单词按钮明显，标签选择采用下拉框+输入框结合方式）  

- 首次使用时提供简易操作指引（如“点击+号添加单词”“点击标签可搜索”）  

- 所有操作响应时间≤1秒（如添加、编辑、搜索、导出）  



### 3.2 兼容性



- 支持Chrome（≥90）、Edge（≥90）、Firefox（≥88）、Safari（≥14）最新稳定版本  

- 适配桌面端屏幕（分辨率≥1366×768），暂不支持移动端  



### 3.3 数据安全性



- 所有数据存储在浏览器localStorage中，仅本地可见，不涉及网络传输  

- 支持手动清空数据（需二次确认）  



### 3.4 稳定性



- 避免因重复操作（如连续点击添加按钮）导致的数据错乱  

- 刷新页面后，所有数据（单词、标签、复习记录）保持一致  



## 4. 数据模型



### 4.1 单词表（words）



|字段名|类型|说明|
|---|---|---|
|id|Number|单词序号（唯一，自增）|
|word|String|英语单词（非空）|
|meaning|String|中文释义（非空）|
|review1|String|第1次复习状态（默认空，可填“已完成”“未完成”）|
|review2|String|第2次复习状态（同review1）|
|review3|String|第3次复习状态（同review1）|
|review4|String|第4次复习状态（同review1）|
|review5|String|第5次复习状态（同review1）|
|review6|String|第6次复习状态（同review1）|
|createTime|String|创建时间（YYYY-MM-DD HH:MM）|


### 4.2 标签表（tags）



|字段名|类型|说明|
|---|---|---|
|tagId|Number|标签ID（唯一，自增）|
|tagName|String|标签名称（唯一，非空）|


### 4.3 单词-标签关联表（wordTags）



|字段名|类型|说明|
|---|---|---|
|id|Number|关联ID（唯一，自增）|
|wordId|Number|单词ID（关联words表的id）|
|tagId|Number|标签ID（关联tags表的tagId）|


## 5. 界面设计概要



### 5.1 主页面（单词列表页）



- 顶部：系统标题“单词管理系统”+ 操作按钮（“添加单词”“标签管理”“导出Excel”）  

- 中部：  

    - 搜索区：标签选择框（支持多选）+“搜索”按钮+“清空筛选”按钮  

    - 单词列表：表格形式展示单词序号、单词、中文释义、关联标签（以逗号分隔）、第1-6次复习状态，每行末尾有“编辑”“删除”按钮  

- 底部：数据统计（“共X个单词”）  



### 5.2 添加/编辑单词页



- 以弹窗形式展示，包含字段：  

    - 单词输入框（编辑时默认填充当前单词）  

    - 中文释义输入框（编辑时默认填充当前释义）  

    - 标签选择区：已创建标签复选框+“添加新标签”输入框（支持输入新标签名称）  

    - 复习记录区（仅编辑时显示）：第1-6次复习状态下拉选择（“已完成”“未完成”“空”）  

    - 操作按钮：“保存”“取消”  



### 5.3 标签管理页



- 以弹窗形式展示，包含：  

    - 标签列表：展示标签名称、关联单词数，每行末尾有“编辑”“删除”按钮  

    - 操作按钮：“添加标签”（弹窗内新增输入框）、“关闭”  



## 6. 导出功能细节



- 导出逻辑：读取words表数据，按“单词序号”升序排序，映射为Excel行数据  

- 复习记录字段：若用户未填写，导出时显示为空单元格；若填写“已完成”“未完成”，则直接导出对应文本  

- 技术实现：使用前端Excel处理库（如js-xlsx），在浏览器端完成数据转换与文件生成，无需后端参与  



## 7. 验收标准



1. 单词管理：可成功添加、编辑、删除单词，且操作后数据实时更新  

2. 标签管理：可创建、编辑、删除标签，标签与单词的关联关系正确维护  

3. 搜索功能：选择标签后，搜索结果仅包含关联所有选中标签的单词  

4. 导出功能：导出的Excel文件表头、字段顺序、内容与系统数据一致，文件可正常打开  

5. 兼容性：在指定浏览器中所有功能正常运行，无界面错乱或功能失效  

6. 数据稳定性：刷新页面后，所有数据（单词、标签、复习记录）保持完整一致  



---



本文档明确了浏览器端单词管理系统的功能、非功能需求及实现规范，可作为通过Cursor生成系统的依据。如需调整细节，可根据实际场景补充说明。
> （注：文档部分内容可能由 AI 生成）