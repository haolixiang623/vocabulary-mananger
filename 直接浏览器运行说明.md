# 直接浏览器运行说明

为了支持直接在浏览器中打开应用（通过 `file://` 协议），我们对项目进行了以下修改：

## 修改内容

### 1. 移除 ES 模块导入/导出
- 将所有 `.js` 文件从 ES 模块（`import/export`）改为全局变量方式
- 主要修改的文件：
  - `config.js`: 改为使用 `window.APP_CONFIG`
  - `supabase-client.js`: 改为使用 `window.supabaseClient`
  - `auth.js`: 改为使用 `window.authUtils`
  - `app.js`: 移除所有导入，使用全局对象

### 2. 更新 HTML 脚本引用
- 将 `<script type="module">` 改为普通 `<script>`
- 按依赖顺序加载脚本文件
- 暂时注释掉非核心服务脚本，避免导入错误

### 3. 添加错误处理
- 增加了更好的错误捕获和提示机制
- 当模块未正确加载时提供降级方案

## 使用方法

### 方法一：直接双击打开
1. 找到项目目录中的 `index.html` 文件
2. 直接双击该文件，或右键选择 "打开方式" -> 选择您的浏览器
3. 应用应该能够加载登录界面（某些功能可能受限）

### 方法二：使用测试页面
1. 打开项目目录中的 `test-direct-open.html` 文件
2. 点击页面上的 "打开主应用" 按钮

## 注意事项

1. **功能限制**：
   - 由于浏览器的安全限制，直接文件访问模式下某些功能（如Supabase数据库连接）可能无法正常工作
   - 您可能会看到一些关于CORS的警告，但登录界面应该能够正常显示

2. **推荐方案**：
   - 对于完整功能体验，仍然推荐使用HTTP服务器方式
   - 您可以使用已配置的 `python -m http.server 8000` 命令启动本地服务器
   - 然后通过 `http://localhost:8000` 访问应用

3. **测试说明**：
   - 登录界面应该能够正常显示
   - 登录功能可能无法连接到Supabase服务器（除非网络环境支持）
   - 错误信息会通过toast提示或错误弹窗显示

## 恢复方法

如果您想恢复到原来的模块方式，可以：
1. 取消注释 `index.html` 中的所有脚本引用
2. 将 `<script>` 改回 `<script type="module">`
3. 恢复各个JS文件中的 `import/export` 语法

---

如有问题，请查看浏览器控制台（F12）获取详细错误信息。